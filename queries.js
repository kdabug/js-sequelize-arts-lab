const { Museum, Artwork, Artist } = require('./models');

const main = async () => {
  // Write a Sequelize query to find the Whitney from the database.

  // Use the `getArtworks()` method and log out the name 
  // of each artwork at the Whitney.

  // Write a Sequelize query to find Early Sunday Morning.

  // For that artwork, use the `getArtist()` method and log out
  // the name of the artist that made it.

  // A new Museum is opening up, "Eric's Museum of Stickers"

  // Based on an article about naming conventions, Eric decides 
  // to rename the museum. Update the name to "Eric's Museum of Curious Stickers"
  // in the database.

  // Eric's museum, although appreciated by a niche population, 
  // doesn't make enough to cover the costs of a Lower East Side
  // storefront. Remove the museum from the database.

  // Write a query to find artworks from before 1950.

  // You decide to take a class in modern art history, and want to
  // go see some relevant artworks.
  // Write a query to find artworks created after 1940 made out of Oil Paint.
};

main();